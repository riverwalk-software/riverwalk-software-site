---
import { YouTube } from "astro-embed";

interface Props {
  id: string;
  classes?: string;
}

const { id, classes = "" } = Astro.props;
---

<YouTube
  {id}
  title="Learn more"
  posterQuality="high"
  class={`min-w-3xl ${classes}`}
/>
